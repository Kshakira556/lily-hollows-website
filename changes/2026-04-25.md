# Changes (2026-04-25)

This folder tracks all edits made during the 'fix outstanding issues' pass.

## Notes
- Goal: fix broken/unfinished interactions without refactors.
- Placeholder content ignored; behavior fixes prioritized.

## Booking fixes
- Fixed hero background rendering (used style backgroundImage instead of data-bg) in src/pages/Booking.tsx.
- Implemented booking form submit via mailto: to the CMS-provided contact email in src/pages/Booking.tsx.

## Contact fixes
- Implemented contact form submit via mailto: using the CMS-provided contact email in src/pages/Contact.tsx.
- Social icons open in new tab when the CMS provides a real URL (no behavior change for '#') in src/pages/Contact.tsx.

## Fan club fixes
- Implemented fan club form submit via mailto: (fetches contact email from CMS) in src/pages/FanClub.tsx.

## Home page fixes
- Latest video card now opens CMS-provided latestVideo.videoUrl when present in src/pages/Index.tsx.
- Newsletter form now triggers a mailto: signup (email sourced from CMS contact page) in src/pages/Index.tsx.

## Videos fixes
- Video tiles now open CMS-provided videoUrl when present in src/pages/Videos.tsx.

## Shows fixes
- Tickets button now links to CMS-provided ticketUrl when present; otherwise it is disabled in src/pages/Shows.tsx.

## Store fixes
- Store CTA now links to CMS-provided productUrl when present; otherwise it is disabled (no cart implementation) in src/pages/Store.tsx.

## EPK fixes
- Featured track play button now links to CMS-provided audioUrl when present in src/pages/EPK.tsx.

## Encoding cleanup
- No changes needed in src/components/Footer.tsx (already uses proper '·' and '©').

- Verified src/pages/Music.tsx already uses proper '·' separator.

## Verification
- Ran npm run build (passes).

- Made homepage featured music play button navigate to /music (removes no-op button) in src/pages/Index.tsx.

- Footer social icons no longer jump to top when still '#'; real URLs open in a new tab in src/components/Footer.tsx.

- Contact social icons no longer jump to top when still '#'; real URLs open in a new tab in src/pages/Contact.tsx.

- Re-ran npm run build after link fixes (passes).

## EPK Color Alignment
- Updated CSS color tokens + gradients to match EPK palette (dusty rose / warm cream / espresso) in src/index.css.

- Re-ran npm run build after color token update (passes).

## Dusty Pink Adjustment
- Updated the site's pink primary token and its direct usages to a more muted dusty pink in src/index.css.

